// Dependencia del módulo :library (simplertmp) requerida por RtmpActivity.
// Se aplica desde capacitor-cordova-android-plugins/build.gradle en cada cap sync.
dependencies {
    implementation project(':library')
}
